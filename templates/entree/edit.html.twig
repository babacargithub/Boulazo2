{% extends 'entree/main_layout.html.twig' %}

{% block content_body %}
    <h1>Modifier entrée</h1>
    <div class="row">
        <div class="col-md-6"> {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Modifier"/>
        {{ form_end(edit_form) }}</div>
    </div>





        <div class="col-12 ">
            <div class="col-md-3">
                <a href="{{ path('entree_index') }}">Retour à la liste
                </a>
                {{ form_start(delete_form) }}
                <input type="submit" value="Supprimer">
                {{ form_end(delete_form) }}
            </div>

        </div>

{% endblock %}
