{% extends 'entree/main_layout.html.twig' %}

{% block content_body %}
    <h1>Créer un nouveau type d'entrée</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Enregistrer" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('type_entree_index') }}">Retour à la liste</a>
        </li>
    </ul>
{% endblock %}
